---
trigger: always_on
---

- Next.js to handle the front-end and backend
- Use REST api, one API call in the page that passes the data to suitable components
- PostgreSQL and Drizzle-ORM as the database
- seed database by default with proposed content
- do not write any unit tests
- Popins font


# Assets & Icons:

Images: Immediately download images to "public/" using curl as you encounter them. Use clear naming (e.g., hero-bg.png).
Icons: Use "lucide-react" as the default icon library unless the design requires a custom, complex SVG.