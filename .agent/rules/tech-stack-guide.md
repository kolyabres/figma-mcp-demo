---
trigger: always_on
---

- next.js to handles the front-end and backend
- use REST api, one API call in the page which pass the data to suitable components
- postgreSQL and drizzle-orm as database
- seed databse by default with proposed content
- do not write any tests
- popins font


#Assets & Icons:
Images: Immediately download images to public/ using curl as you encounter them. Use clear naming (e.g., hero-bg.png).
Icons: Use lucide-react as the default for icons unless the design requires a custom complex SVG.